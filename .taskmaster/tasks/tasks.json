{
  "work-history-improvement": {
    "tasks": [
      {
        "id": 1,
        "title": "테이블 UI 기본 구조 설계",
        "description": "카드 그리드에서 테이블 리스트로 UI 구조를 변경하기 위한 기본 레이아웃 설계",
        "details": "src/app/history/page.tsx 파일을 리팩토링하여 테이블 기반 UI로 변경합니다. 테이블 헤더는 '작업일', '고객/장비', 'ECU정보', 'ACU정보', '상태', '금액', '작업' 컬럼으로 구성합니다. Tailwind CSS를 활용하여 반응형 테이블을 구현하고, 기본적인 테이블 구조는 다음과 같이 구현합니다:\n\n```tsx\nimport { useState, useEffect } from 'react';\nimport { fetchWorkRecords } from '@/lib/work-records';\n\nconst HistoryPage = () => {\n  const [records, setRecords] = useState([]);\n  const [loading, setLoading] = useState(true);\n\n  useEffect(() => {\n    const loadData = async () => {\n      setLoading(true);\n      try {\n        const data = await fetchWorkRecords();\n        setRecords(data);\n      } catch (error) {\n        console.error('데이터 로딩 오류:', error);\n      } finally {\n        setLoading(false);\n      }\n    };\n    \n    loadData();\n  }, []);\n\n  return (\n    <div className=\"container mx-auto px-4 py-8\">\n      <h1 className=\"text-2xl font-bold mb-6\">작업 이력</h1>\n      \n      <div className=\"overflow-x-auto\">\n        <table className=\"min-w-full bg-white border border-gray-200\">\n          <thead>\n            <tr className=\"bg-gray-100\">\n              <th className=\"py-3 px-4 border-b text-left\">작업일</th>\n              <th className=\"py-3 px-4 border-b text-left\">고객/장비</th>\n              <th className=\"py-3 px-4 border-b text-left\">🔧 ECU 정보</th>\n              <th className=\"py-3 px-4 border-b text-left\">⚙️ ACU 정보</th>\n              <th className=\"py-3 px-4 border-b text-left\">상태</th>\n              <th className=\"py-3 px-4 border-b text-left\">금액</th>\n              <th className=\"py-3 px-4 border-b text-left\">작업</th>\n            </tr>\n          </thead>\n          <tbody>\n            {loading ? (\n              <tr>\n                <td colSpan={7} className=\"py-4 text-center\">로딩 중...</td>\n              </tr>\n            ) : records.length === 0 ? (\n              <tr>\n                <td colSpan={7} className=\"py-4 text-center\">작업 이력이 없습니다.</td>\n              </tr>\n            ) : (\n              records.map((record) => (\n                <tr key={record.id} className=\"hover:bg-gray-50\">\n                  {/* 데이터 셀은 다음 작업에서 구현 */}\n                </tr>\n              ))\n            )}\n          </tbody>\n        </table>\n      </div>\n    </div>\n  );\n};\n\nexport default HistoryPage;\n```\n\n테이블 구조는 Next.js 14의 App Router 구조에 맞게 설계하고, 모바일 환경에서도 사용 가능하도록 overflow-x-auto를 적용합니다.",
        "testStrategy": "1. 테이블 구조가 정확히 렌더링되는지 확인\n2. 반응형 디자인이 다양한 화면 크기에서 제대로 작동하는지 테스트\n3. 테이블 헤더가 명세에 맞게 정확히 표시되는지 확인\n4. 로딩 상태와 빈 데이터 상태가 적절히 표시되는지 검증",
        "priority": "high",
        "dependencies": [],
        "status": "pending",
        "subtasks": []
      },
      {
        "id": 2,
        "title": "데이터 매핑 및 테이블 행 구현",
        "description": "기존 데이터 구조를 새로운 테이블 UI에 맞게 매핑하고 테이블 행 컴포넌트 구현",
        "details": "기존 work-records API에서 가져온 데이터를 테이블 행에 매핑합니다. 각 컬럼에 맞는 데이터 포맷팅 로직을 구현하고, 행 클릭 시 상세 모달을 열도록 설정합니다.\n\n```tsx\n// 테이블 행 컴포넌트 구현\nconst WorkRecordRow = ({ record, onRowClick }) => {\n  // 날짜 포맷팅\n  const formattedDate = new Date(record.created_at).toLocaleDateString('ko-KR', {\n    year: 'numeric',\n    month: 'long',\n    day: 'numeric'\n  });\n\n  return (\n    <tr \n      key={record.id} \n      className=\"hover:bg-gray-50 cursor-pointer border-b\" \n      onClick={() => onRowClick(record)}\n    >\n      <td className=\"py-3 px-4\">{formattedDate}</td>\n      <td className=\"py-3 px-4\">\n        <div className=\"font-medium\">{record.customer_name}</div>\n        <div className=\"text-sm text-gray-500\">{record.vehicle_model}</div>\n      </td>\n      <td className=\"py-3 px-4\">\n        {/* ECU 정보는 다음 작업에서 구현 */}\n        <div>기본 ECU 정보</div>\n      </td>\n      <td className=\"py-3 px-4\">\n        {/* ACU 정보는 다음 작업에서 구현 */}\n        <div>기본 ACU 정보</div>\n      </td>\n      <td className=\"py-3 px-4\">\n        <span className={`px-2 py-1 rounded-full text-xs ${getStatusColor(record.status)}`}>\n          {record.status}\n        </span>\n      </td>\n      <td className=\"py-3 px-4\">\n        {new Intl.NumberFormat('ko-KR', { style: 'currency', currency: 'KRW' }).format(record.amount)}\n      </td>\n      <td className=\"py-3 px-4\">\n        <button className=\"text-blue-500 hover:text-blue-700\">\n          상세보기\n        </button>\n      </td>\n    </tr>\n  );\n};\n\n// 상태에 따른 색상 반환 함수\nconst getStatusColor = (status) => {\n  switch(status) {\n    case '완료': return 'bg-green-100 text-green-800';\n    case '진행중': return 'bg-blue-100 text-blue-800';\n    case '대기중': return 'bg-yellow-100 text-yellow-800';\n    default: return 'bg-gray-100 text-gray-800';\n  }\n};\n```\n\n메인 컴포넌트에서는 다음과 같이 통합합니다:\n\n```tsx\n// 메인 컴포넌트 내부\nconst [selectedRecord, setSelectedRecord] = useState(null);\n\nconst handleRowClick = (record) => {\n  setSelectedRecord(record);\n  // 모달 열기 로직 추가 예정\n};\n\n// 테이블 tbody 내부\n{\n  records.map((record) => (\n    <WorkRecordRow \n      key={record.id} \n      record={record} \n      onRowClick={handleRowClick} \n    />\n  ))\n}\n```\n\n이 구현은 기존 데이터 구조를 유지하면서 UI만 변경하는 접근 방식을 사용합니다.",
        "testStrategy": "1. 모든 데이터 필드가 올바르게 매핑되는지 확인\n2. 날짜, 금액 등의 포맷팅이 한국어 로케일에 맞게 표시되는지 검증\n3. 행 클릭 이벤트가 정상 작동하는지 테스트\n4. 다양한 상태값에 대해 올바른 색상이 적용되는지 확인\n5. 빈 값이나 null 값이 있는 경우 UI가 깨지지 않는지 테스트",
        "priority": "high",
        "dependencies": [
          1
        ],
        "status": "pending",
        "subtasks": []
      },
      {
        "id": 3,
        "title": "ECU 정보 표시 컴포넌트 구현",
        "description": "ECU 튜닝 정보를 전용 컬럼에 표시하는 컴포넌트 개발",
        "details": "ECU 정보를 명확하게 표시하기 위한 전용 컴포넌트를 구현합니다. 파란색 계열의 색상 코딩을 적용하고, 제조사, 모델, 튜닝 단계 정보를 구조화하여 표시합니다.\n\n```tsx\nconst ECUInfoCell = ({ ecuData }) => {\n  // ECU 데이터가 없는 경우 처리\n  if (!ecuData || Object.keys(ecuData).length === 0) {\n    return <span className=\"text-gray-400 italic\">N/A</span>;\n  }\n\n  return (\n    <div className=\"flex flex-col\">\n      <div className=\"flex items-center\">\n        <span className=\"w-2 h-2 rounded-full bg-blue-500 mr-2\"></span>\n        <span className=\"font-medium text-blue-700\">{ecuData.manufacturer}</span>\n      </div>\n      <div className=\"text-sm text-gray-600 ml-4\">{ecuData.model || 'Unknown'}</div>\n      {ecuData.tuning_stage && (\n        <div className=\"mt-1 px-2 py-0.5 bg-blue-100 text-blue-800 rounded text-xs inline-block ml-4\">\n          Stage {ecuData.tuning_stage}\n        </div>\n      )}\n    </div>\n  );\n};\n```\n\n이 컴포넌트를 테이블 행에 통합합니다:\n\n```tsx\n// WorkRecordRow 컴포넌트 내부 ECU 정보 셀\n<td className=\"py-3 px-4\">\n  <ECUInfoCell ecuData={record.ecu_data} />\n</td>\n```\n\n데이터 구조가 중첩되어 있거나 다른 형태인 경우, 적절한 데이터 변환 로직을 추가합니다:\n\n```tsx\n// 데이터 변환 예시 (필요한 경우)\nconst transformECUData = (record) => {\n  // 기존 데이터 구조에서 ECU 관련 정보 추출\n  return {\n    manufacturer: record.ecu_manufacturer || record.manufacturer,\n    model: record.ecu_model || record.model_name,\n    tuning_stage: record.tuning_stage || record.stage\n  };\n};\n\n// 사용 예시\n<ECUInfoCell ecuData={transformECUData(record)} />\n```\n\n이 구현은 데이터가 없는 경우에도 UI가 깨지지 않도록 방어적 코딩 방식을 적용합니다.",
        "testStrategy": "1. ECU 데이터가 있는 경우와 없는 경우 모두 올바르게 표시되는지 확인\n2. 색상 코딩이 디자인 명세에 맞게 적용되었는지 검증\n3. 다양한 데이터 구조에 대해 변환 로직이 정상 작동하는지 테스트\n4. 튜닝 단계(Stage) 정보가 시각적으로 돋보이게 표시되는지 확인\n5. 모바일 화면에서도 정보가 명확히 표시되는지 테스트",
        "priority": "medium",
        "dependencies": [
          2
        ],
        "status": "pending",
        "subtasks": []
      },
      {
        "id": 4,
        "title": "ACU 정보 표시 컴포넌트 구현",
        "description": "ACU 튜닝 정보를 전용 컬럼에 표시하는 컴포넌트 개발",
        "details": "ACU 정보를 명확하게 표시하기 위한 전용 컴포넌트를 구현합니다. 초록색 계열의 색상 코딩을 적용하고, 제조사, 모델, 활성화 상태 정보를 구조화하여 표시합니다.\n\n```tsx\nconst ACUInfoCell = ({ acuData }) => {\n  // ACU 데이터가 없는 경우 처리\n  if (!acuData || Object.keys(acuData).length === 0) {\n    return <span className=\"text-gray-400 italic\">N/A</span>;\n  }\n\n  return (\n    <div className=\"flex flex-col\">\n      <div className=\"flex items-center\">\n        <span className=\"w-2 h-2 rounded-full bg-green-500 mr-2\"></span>\n        <span className=\"font-medium text-green-700\">{acuData.manufacturer}</span>\n      </div>\n      <div className=\"text-sm text-gray-600 ml-4\">{acuData.model || 'Unknown'}</div>\n      <div className=\"mt-1 ml-4\">\n        <span className={`px-2 py-0.5 rounded text-xs ${acuData.is_active ? 'bg-green-100 text-green-800' : 'bg-gray-100 text-gray-800'}`}>\n          {acuData.is_active ? '활성화' : '비활성화'}\n        </span>\n      </div>\n    </div>\n  );\n};\n```\n\n이 컴포넌트를 테이블 행에 통합합니다:\n\n```tsx\n// WorkRecordRow 컴포넌트 내부 ACU 정보 셀\n<td className=\"py-3 px-4\">\n  <ACUInfoCell acuData={record.acu_data} />\n</td>\n```\n\n데이터 구조가 중첩되어 있거나 다른 형태인 경우, 적절한 데이터 변환 로직을 추가합니다:\n\n```tsx\n// 데이터 변환 예시 (필요한 경우)\nconst transformACUData = (record) => {\n  // 기존 데이터 구조에서 ACU 관련 정보 추출\n  return {\n    manufacturer: record.acu_manufacturer || record.manufacturer,\n    model: record.acu_model || record.model_name,\n    is_active: record.acu_active || record.is_active || false\n  };\n};\n\n// 사용 예시\n<ACUInfoCell acuData={transformACUData(record)} />\n```\n\n이 구현은 ECU 정보 컴포넌트와 시각적으로 구분되면서도 일관된 디자인 언어를 유지합니다.",
        "testStrategy": "1. ACU 데이터가 있는 경우와 없는 경우 모두 올바르게 표시되는지 확인\n2. 초록색 계열 색상 코딩이 디자인 명세에 맞게 적용되었는지 검증\n3. 다양한 데이터 구조에 대해 변환 로직이 정상 작동하는지 테스트\n4. 활성화/비활성화 상태가 시각적으로 명확히 구분되는지 확인\n5. ECU 정보 컴포넌트와 시각적으로 명확히 구분되는지 검증",
        "priority": "medium",
        "dependencies": [
          2
        ],
        "status": "pending",
        "subtasks": []
      },
      {
        "id": 5,
        "title": "작업 상세 모달 리팩토링",
        "description": "기존 WorkDetailModal 컴포넌트를 개선하여 테이블 UI와 통합",
        "details": "기존 WorkDetailModal 컴포넌트를 리팩토링하여 테이블 행 클릭 시 열리도록 하고, ECU/ACU 정보를 분리하여 표시합니다. 모달 내부에 파일 다운로드 섹션을 추가할 준비를 합니다.\n\n```tsx\nimport { Dialog, Transition } from '@headlessui/react';\nimport { Fragment, useState } from 'react';\n\nconst WorkDetailModal = ({ isOpen, onClose, record }) => {\n  if (!record) return null;\n\n  return (\n    <Transition appear show={isOpen} as={Fragment}>\n      <Dialog as=\"div\" className=\"relative z-10\" onClose={onClose}>\n        <Transition.Child\n          as={Fragment}\n          enter=\"ease-out duration-300\"\n          enterFrom=\"opacity-0\"\n          enterTo=\"opacity-100\"\n          leave=\"ease-in duration-200\"\n          leaveFrom=\"opacity-100\"\n          leaveTo=\"opacity-0\"\n        >\n          <div className=\"fixed inset-0 bg-black bg-opacity-25\" />\n        </Transition.Child>\n\n        <div className=\"fixed inset-0 overflow-y-auto\">\n          <div className=\"flex min-h-full items-center justify-center p-4 text-center\">\n            <Transition.Child\n              as={Fragment}\n              enter=\"ease-out duration-300\"\n              enterFrom=\"opacity-0 scale-95\"\n              enterTo=\"opacity-100 scale-100\"\n              leave=\"ease-in duration-200\"\n              leaveFrom=\"opacity-100 scale-100\"\n              leaveTo=\"opacity-0 scale-95\"\n            >\n              <Dialog.Panel className=\"w-full max-w-2xl transform overflow-hidden rounded-2xl bg-white p-6 text-left align-middle shadow-xl transition-all\">\n                <Dialog.Title as=\"h3\" className=\"text-lg font-medium leading-6 text-gray-900 border-b pb-2\">\n                  작업 상세 정보\n                </Dialog.Title>\n                \n                <div className=\"mt-4 grid grid-cols-1 md:grid-cols-2 gap-4\">\n                  {/* 기본 정보 섹션 */}\n                  <div className=\"col-span-1 md:col-span-2\">\n                    <h4 className=\"font-medium text-gray-700\">기본 정보</h4>\n                    <div className=\"mt-2 bg-gray-50 p-3 rounded\">\n                      <div className=\"grid grid-cols-2 gap-2\">\n                        <div>\n                          <p className=\"text-sm text-gray-500\">작업일</p>\n                          <p>{new Date(record.created_at).toLocaleDateString('ko-KR')}</p>\n                        </div>\n                        <div>\n                          <p className=\"text-sm text-gray-500\">상태</p>\n                          <p>{record.status}</p>\n                        </div>\n                        <div>\n                          <p className=\"text-sm text-gray-500\">고객명</p>\n                          <p>{record.customer_name}</p>\n                        </div>\n                        <div>\n                          <p className=\"text-sm text-gray-500\">금액</p>\n                          <p>{new Intl.NumberFormat('ko-KR', { style: 'currency', currency: 'KRW' }).format(record.amount)}</p>\n                        </div>\n                      </div>\n                    </div>\n                  </div>\n                  \n                  {/* ECU 정보 섹션 */}\n                  <div>\n                    <h4 className=\"font-medium text-blue-700\">🔧 ECU 정보</h4>\n                    <div className=\"mt-2 bg-blue-50 p-3 rounded\">\n                      {record.ecu_data ? (\n                        <>\n                          <p><span className=\"text-sm text-gray-500\">제조사:</span> {record.ecu_data.manufacturer}</p>\n                          <p><span className=\"text-sm text-gray-500\">모델:</span> {record.ecu_data.model}</p>\n                          <p><span className=\"text-sm text-gray-500\">튜닝 단계:</span> Stage {record.ecu_data.tuning_stage}</p>\n                        </>\n                      ) : (\n                        <p className=\"text-gray-400 italic\">ECU 정보 없음</p>\n                      )}\n                    </div>\n                  </div>\n                  \n                  {/* ACU 정보 섹션 */}\n                  <div>\n                    <h4 className=\"font-medium text-green-700\">⚙️ ACU 정보</h4>\n                    <div className=\"mt-2 bg-green-50 p-3 rounded\">\n                      {record.acu_data ? (\n                        <>\n                          <p><span className=\"text-sm text-gray-500\">제조사:</span> {record.acu_data.manufacturer}</p>\n                          <p><span className=\"text-sm text-gray-500\">모델:</span> {record.acu_data.model}</p>\n                          <p><span className=\"text-sm text-gray-500\">상태:</span> {record.acu_data.is_active ? '활성화' : '비활성화'}</p>\n                        </>\n                      ) : (\n                        <p className=\"text-gray-400 italic\">ACU 정보 없음</p>\n                      )}\n                    </div>\n                  </div>\n                  \n                  {/* 파일 다운로드 섹션 자리 (다음 작업에서 구현) */}\n                  <div className=\"col-span-1 md:col-span-2 mt-4\">\n                    <h4 className=\"font-medium text-gray-700\">파일</h4>\n                    <div className=\"mt-2 bg-gray-50 p-3 rounded\">\n                      <p className=\"text-gray-400 italic\">파일 다운로드 섹션 (준비 중)</p>\n                    </div>\n                  </div>\n                </div>\n\n                <div className=\"mt-6 flex justify-end\">\n                  <button\n                    type=\"button\"\n                    className=\"inline-flex justify-center rounded-md border border-transparent bg-blue-100 px-4 py-2 text-sm font-medium text-blue-900 hover:bg-blue-200 focus:outline-none focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:ring-offset-2\"\n                    onClick={onClose}\n                  >\n                    닫기\n                  </button>\n                </div>\n              </Dialog.Panel>\n            </Transition.Child>\n          </div>\n        </div>\n      </Dialog>\n    </Transition>\n  );\n};\n\nexport default WorkDetailModal;\n```\n\n메인 페이지에서 모달을 통합합니다:\n\n```tsx\n// 메인 컴포넌트 내부\nconst [isModalOpen, setIsModalOpen] = useState(false);\nconst [selectedRecord, setSelectedRecord] = useState(null);\n\nconst handleRowClick = (record) => {\n  setSelectedRecord(record);\n  setIsModalOpen(true);\n};\n\nconst closeModal = () => {\n  setIsModalOpen(false);\n};\n\n// JSX 반환부 하단에 추가\n<WorkDetailModal \n  isOpen={isModalOpen} \n  onClose={closeModal} \n  record={selectedRecord} \n/>\n```\n\n이 구현은 @headlessui/react 라이브러리를 사용하여 접근성이 좋은 모달을 구현합니다.",
        "testStrategy": "1. 테이블 행 클릭 시 모달이 정상적으로 열리는지 확인\n2. 모달 내 ECU/ACU 정보가 명확히 구분되어 표시되는지 검증\n3. 모달이 모바일 화면에서도 적절히 표시되는지 테스트\n4. 모달 닫기 기능이 정상 작동하는지 확인\n5. 키보드 접근성(Esc 키로 닫기 등)이 제대로 구현되었는지 테스트",
        "priority": "high",
        "dependencies": [
          3,
          4
        ],
        "status": "pending",
        "subtasks": []
      },
      {
        "id": 6,
        "title": "파일 다운로드 컴포넌트 통합",
        "description": "기존 FileDownloadSection 컴포넌트를 상세 모달에 통합하여 파일 다운로드 기능 구현",
        "details": "기존 FileDownloadSection 컴포넌트를 작업 상세 모달에 통합하여 업로드된 파일을 다운로드할 수 있는 기능을 구현합니다. Supabase Storage API를 활용하여 파일 메타데이터를 로드하고 다운로드 링크를 생성합니다.\n\n```tsx\nimport { useState, useEffect } from 'react';\nimport { supabase } from '@/lib/supabaseClient';\n\nconst FileDownloadSection = ({ recordId }) => {\n  const [files, setFiles] = useState([]);\n  const [loading, setLoading] = useState(true);\n  const [error, setError] = useState(null);\n\n  useEffect(() => {\n    const fetchFiles = async () => {\n      if (!recordId) return;\n      \n      setLoading(true);\n      try {\n        // 파일 메타데이터 가져오기\n        const { data, error } = await supabase\n          .from('file_metadata')\n          .select('*')\n          .eq('work_record_id', recordId);\n          \n        if (error) throw error;\n        setFiles(data || []);\n      } catch (err) {\n        console.error('파일 로딩 오류:', err);\n        setError('파일 정보를 불러오는 중 오류가 발생했습니다.');\n      } finally {\n        setLoading(false);\n      }\n    };\n    \n    fetchFiles();\n  }, [recordId]);\n\n  const handleDownload = async (file) => {\n    try {\n      // Supabase Storage에서 파일 URL 가져오기\n      const { data, error } = await supabase\n        .storage\n        .from('work_files')\n        .createSignedUrl(file.storage_path, 60); // 60초 유효한 URL 생성\n        \n      if (error) throw error;\n      \n      // 파일 다운로드 링크 열기\n      window.open(data.signedUrl, '_blank');\n    } catch (err) {\n      console.error('파일 다운로드 오류:', err);\n      alert('파일 다운로드 중 오류가 발생했습니다.');\n    }\n  };\n\n  return (\n    <div className=\"mt-4\">\n      <h4 className=\"font-medium text-gray-700\">파일 다운로드</h4>\n      <div className=\"mt-2 bg-gray-50 p-3 rounded\">\n        {loading ? (\n          <p className=\"text-center py-2\">파일 정보 로딩 중...</p>\n        ) : error ? (\n          <p className=\"text-red-500\">{error}</p>\n        ) : files.length === 0 ? (\n          <p className=\"text-gray-400 italic\">업로드된 파일이 없습니다.</p>\n        ) : (\n          <div className=\"space-y-2\">\n            {files.map((file) => (\n              <div key={file.id} className=\"flex items-center justify-between bg-white p-2 rounded border\">\n                <div className=\"flex items-center\">\n                  <svg className=\"w-5 h-5 text-gray-500 mr-2\" fill=\"currentColor\" viewBox=\"0 0 20 20\">\n                    <path fillRule=\"evenodd\" d=\"M4 4a2 2 0 012-2h4.586A2 2 0 0112 2.586L15.414 6A2 2 0 0116 7.414V16a2 2 0 01-2 2H6a2 2 0 01-2-2V4z\" clipRule=\"evenodd\" />\n                  </svg>\n                  <span className=\"truncate max-w-xs\">{file.original_name}</span>\n                </div>\n                <button \n                  onClick={() => handleDownload(file)}\n                  className=\"ml-2 text-blue-500 hover:text-blue-700\"\n                >\n                  다운로드\n                </button>\n              </div>\n            ))}\n            \n            {files.length > 1 && (\n              <div className=\"mt-3 text-right\">\n                <button className=\"px-3 py-1 bg-blue-100 text-blue-800 rounded hover:bg-blue-200\">\n                  전체 다운로드\n                </button>\n              </div>\n            )}\n          </div>\n        )}\n      </div>\n    </div>\n  );\n};\n\nexport default FileDownloadSection;\n```\n\n이 컴포넌트를 WorkDetailModal에 통합합니다:\n\n```tsx\n// WorkDetailModal 컴포넌트 내부 파일 다운로드 섹션 부분\n<div className=\"col-span-1 md:col-span-2 mt-4\">\n  <FileDownloadSection recordId={record.id} />\n</div>\n```\n\n전체 파일 다운로드 기능은 선택적으로 구현할 수 있으며, 이를 위해서는 JSZip 라이브러리를 활용하여 클라이언트 측에서 ZIP 파일을 생성하는 방식을 사용할 수 있습니다.",
        "testStrategy": "1. 파일 메타데이터가 정상적으로 로드되는지 확인\n2. 개별 파일 다운로드 기능이 정상 작동하는지 테스트\n3. 다양한 파일 유형(이미지, 문서 등)에 대해 다운로드가 정상 작동하는지 확인\n4. 파일이 없는 경우 적절한 메시지가 표시되는지 검증\n5. 로딩 상태와 에러 상태가 사용자에게 명확히 표시되는지 테스트\n6. (선택) 전체 파일 다운로드 기능이 정상 작동하는지 확인",
        "priority": "high",
        "dependencies": [
          5
        ],
        "status": "pending",
        "subtasks": []
      },
      {
        "id": 7,
        "title": "전체 파일 ZIP 다운로드 기능 구현",
        "description": "여러 파일을 한 번에 ZIP 형식으로 다운로드할 수 있는 기능 구현",
        "details": "JSZip 라이브러리를 활용하여 선택한 모든 파일을 하나의 ZIP 파일로 묶어 다운로드하는 기능을 구현합니다. 이 기능은 사용자가 여러 파일을 개별적으로 다운로드하는 번거로움을 줄여줍니다.\n\n```tsx\nimport JSZip from 'jszip';\nimport { saveAs } from 'file-saver';\n\n// FileDownloadSection 컴포넌트 내부에 추가\nconst handleBulkDownload = async () => {\n  if (files.length === 0) return;\n  \n  try {\n    // 진행 상태 표시 (선택적)\n    setLoading(true);\n    \n    const zip = new JSZip();\n    const promises = [];\n    \n    // 각 파일에 대한 다운로드 Promise 생성\n    for (const file of files) {\n      const promise = new Promise(async (resolve, reject) => {\n        try {\n          // Supabase Storage에서 파일 URL 가져오기\n          const { data, error } = await supabase\n            .storage\n            .from('work_files')\n            .createSignedUrl(file.storage_path, 60);\n            \n          if (error) throw error;\n          \n          // 파일 다운로드 및 ZIP에 추가\n          const response = await fetch(data.signedUrl);\n          const blob = await response.blob();\n          zip.file(file.original_name, blob);\n          resolve();\n        } catch (err) {\n          console.error(`파일 '${file.original_name}' 다운로드 오류:`, err);\n          reject(err);\n        }\n      });\n      \n      promises.push(promise);\n    }\n    \n    // 모든 파일 다운로드 완료 대기\n    await Promise.all(promises);\n    \n    // ZIP 파일 생성 및 다운로드\n    const content = await zip.generateAsync({ type: 'blob' });\n    const timestamp = new Date().toISOString().replace(/[:.]/g, '-');\n    saveAs(content, `work-files-${recordId}-${timestamp}.zip`);\n  } catch (err) {\n    console.error('일괄 다운로드 오류:', err);\n    alert('파일 일괄 다운로드 중 오류가 발생했습니다.');\n  } finally {\n    setLoading(false);\n  }\n};\n\n// 전체 다운로드 버튼 부분 수정\n{files.length > 1 && (\n  <div className=\"mt-3 text-right\">\n    <button \n      onClick={handleBulkDownload}\n      disabled={loading}\n      className=\"px-3 py-1 bg-blue-100 text-blue-800 rounded hover:bg-blue-200 disabled:opacity-50\"\n    >\n      {loading ? '준비 중...' : '전체 다운로드'}\n    </button>\n  </div>\n)}\n```\n\n필요한 패키지를 설치합니다:\n\n```bash\nnpm install jszip file-saver\n# 또는\nyarn add jszip file-saver\n```\n\n타입스크립트를 사용하는 경우 타입 정의도 설치합니다:\n\n```bash\nnpm install -D @types/file-saver\n# 또는\nyarn add -D @types/file-saver\n```\n\n이 구현은 클라이언트 측에서 ZIP 파일을 생성하므로 서버 부하를 줄이고, 사용자에게 즉각적인 피드백을 제공합니다.",
        "testStrategy": "1. 여러 파일이 있는 경우 ZIP 다운로드 버튼이 정상적으로 표시되는지 확인\n2. ZIP 파일 생성 및 다운로드가 정상 작동하는지 테스트\n3. 다양한 파일 유형과 크기에 대해 ZIP 생성이 정상 작동하는지 확인\n4. 다운로드 중 로딩 상태가 사용자에게 명확히 표시되는지 테스트\n5. 오류 발생 시 적절한 오류 메시지가 표시되는지 검증\n6. 대용량 파일 처리 시 성능 테스트",
        "priority": "medium",
        "dependencies": [
          6
        ],
        "status": "pending",
        "subtasks": []
      },
      {
        "id": 8,
        "title": "테이블 정렬 및 필터링 기능 구현",
        "description": "작업 이력 테이블에 정렬 및 필터링 기능을 추가하여 사용자 경험 향상",
        "details": "테이블 헤더 클릭으로 정렬하고, 필터 컨트롤을 통해 ECU/ACU 타입, 고객, 기간 등으로 필터링할 수 있는 기능을 구현합니다. 이를 통해 사용자가 대량의 작업 이력 데이터를 효율적으로 탐색할 수 있습니다.\n\n```tsx\nimport { useState, useEffect } from 'react';\nimport { fetchWorkRecords } from '@/lib/work-records';\n\nconst HistoryPage = () => {\n  const [records, setRecords] = useState([]);\n  const [filteredRecords, setFilteredRecords] = useState([]);\n  const [loading, setLoading] = useState(true);\n  \n  // 정렬 상태\n  const [sortField, setSortField] = useState('created_at');\n  const [sortDirection, setSortDirection] = useState('desc'); // 'asc' 또는 'desc'\n  \n  // 필터 상태\n  const [filters, setFilters] = useState({\n    ecuType: 'all', // 'all', 'with-ecu', 'without-ecu'\n    acuType: 'all', // 'all', 'with-acu', 'without-acu'\n    customer: '',\n    dateRange: {\n      start: '',\n      end: ''\n    }\n  });\n\n  // 데이터 로딩\n  useEffect(() => {\n    const loadData = async () => {\n      setLoading(true);\n      try {\n        const data = await fetchWorkRecords();\n        setRecords(data);\n        setFilteredRecords(data);\n      } catch (error) {\n        console.error('데이터 로딩 오류:', error);\n      } finally {\n        setLoading(false);\n      }\n    };\n    \n    loadData();\n  }, []);\n\n  // 정렬 및 필터링 적용\n  useEffect(() => {\n    let result = [...records];\n    \n    // 필터 적용\n    if (filters.ecuType !== 'all') {\n      result = result.filter(record => {\n        if (filters.ecuType === 'with-ecu') return record.ecu_data && Object.keys(record.ecu_data).length > 0;\n        return !record.ecu_data || Object.keys(record.ecu_data).length === 0;\n      });\n    }\n    \n    if (filters.acuType !== 'all') {\n      result = result.filter(record => {\n        if (filters.acuType === 'with-acu') return record.acu_data && Object.keys(record.acu_data).length > 0;\n        return !record.acu_data || Object.keys(record.acu_data).length === 0;\n      });\n    }\n    \n    if (filters.customer) {\n      const searchTerm = filters.customer.toLowerCase();\n      result = result.filter(record => \n        record.customer_name.toLowerCase().includes(searchTerm)\n      );\n    }\n    \n    if (filters.dateRange.start) {\n      const startDate = new Date(filters.dateRange.start);\n      result = result.filter(record => new Date(record.created_at) >= startDate);\n    }\n    \n    if (filters.dateRange.end) {\n      const endDate = new Date(filters.dateRange.end);\n      endDate.setHours(23, 59, 59, 999); // 해당 일자의 마지막 시간으로 설정\n      result = result.filter(record => new Date(record.created_at) <= endDate);\n    }\n    \n    // 정렬 적용\n    result.sort((a, b) => {\n      let valueA = a[sortField];\n      let valueB = b[sortField];\n      \n      // 날짜 필드인 경우 Date 객체로 변환\n      if (sortField === 'created_at') {\n        valueA = new Date(valueA);\n        valueB = new Date(valueB);\n      }\n      \n      // 문자열인 경우 소문자로 변환하여 비교\n      if (typeof valueA === 'string') valueA = valueA.toLowerCase();\n      if (typeof valueB === 'string') valueB = valueB.toLowerCase();\n      \n      if (valueA < valueB) return sortDirection === 'asc' ? -1 : 1;\n      if (valueA > valueB) return sortDirection === 'asc' ? 1 : -1;\n      return 0;\n    });\n    \n    setFilteredRecords(result);\n  }, [records, filters, sortField, sortDirection]);\n\n  // 정렬 토글 핸들러\n  const handleSort = (field) => {\n    if (sortField === field) {\n      // 같은 필드를 다시 클릭하면 정렬 방향 전환\n      setSortDirection(sortDirection === 'asc' ? 'desc' : 'asc');\n    } else {\n      // 다른 필드를 클릭하면 해당 필드로 정렬하고 기본 내림차순 적용\n      setSortField(field);\n      setSortDirection('desc');\n    }\n  };\n\n  // 필터 변경 핸들러\n  const handleFilterChange = (name, value) => {\n    setFilters(prev => {\n      if (name.includes('.')) {\n        // 중첩된 필터 (dateRange.start 등)\n        const [parent, child] = name.split('.');\n        return {\n          ...prev,\n          [parent]: {\n            ...prev[parent],\n            [child]: value\n          }\n        };\n      }\n      return {\n        ...prev,\n        [name]: value\n      };\n    });\n  };\n\n  // 필터 초기화 핸들러\n  const resetFilters = () => {\n    setFilters({\n      ecuType: 'all',\n      acuType: 'all',\n      customer: '',\n      dateRange: {\n        start: '',\n        end: ''\n      }\n    });\n  };\n\n  return (\n    <div className=\"container mx-auto px-4 py-8\">\n      <h1 className=\"text-2xl font-bold mb-6\">작업 이력</h1>\n      \n      {/* 필터 컨트롤 */}\n      <div className=\"mb-6 bg-white p-4 rounded shadow\">\n        <div className=\"grid grid-cols-1 md:grid-cols-4 gap-4\">\n          <div>\n            <label className=\"block text-sm font-medium text-gray-700 mb-1\">ECU 타입</label>\n            <select\n              className=\"w-full border-gray-300 rounded-md shadow-sm focus:border-blue-500 focus:ring-blue-500\"\n              value={filters.ecuType}\n              onChange={(e) => handleFilterChange('ecuType', e.target.value)}\n            >\n              <option value=\"all\">전체</option>\n              <option value=\"with-ecu\">ECU 튜닝 있음</option>\n              <option value=\"without-ecu\">ECU 튜닝 없음</option>\n            </select>\n          </div>\n          \n          <div>\n            <label className=\"block text-sm font-medium text-gray-700 mb-1\">ACU 타입</label>\n            <select\n              className=\"w-full border-gray-300 rounded-md shadow-sm focus:border-blue-500 focus:ring-blue-500\"\n              value={filters.acuType}\n              onChange={(e) => handleFilterChange('acuType', e.target.value)}\n            >\n              <option value=\"all\">전체</option>\n              <option value=\"with-acu\">ACU 튜닝 있음</option>\n              <option value=\"without-acu\">ACU 튜닝 없음</option>\n            </select>\n          </div>\n          \n          <div>\n            <label className=\"block text-sm font-medium text-gray-700 mb-1\">고객명</label>\n            <input\n              type=\"text\"\n              className=\"w-full border-gray-300 rounded-md shadow-sm focus:border-blue-500 focus:ring-blue-500\"\n              placeholder=\"고객명 검색\"\n              value={filters.customer}\n              onChange={(e) => handleFilterChange('customer', e.target.value)}\n            />\n          </div>\n          \n          <div>\n            <label className=\"block text-sm font-medium text-gray-700 mb-1\">기간</label>\n            <div className=\"flex space-x-2\">\n              <input\n                type=\"date\"\n                className=\"w-full border-gray-300 rounded-md shadow-sm focus:border-blue-500 focus:ring-blue-500\"\n                value={filters.dateRange.start}\n                onChange={(e) => handleFilterChange('dateRange.start', e.target.value)}\n              />\n              <span className=\"self-center\">~</span>\n              <input\n                type=\"date\"\n                className=\"w-full border-gray-300 rounded-md shadow-sm focus:border-blue-500 focus:ring-blue-500\"\n                value={filters.dateRange.end}\n                onChange={(e) => handleFilterChange('dateRange.end', e.target.value)}\n              />\n            </div>\n          </div>\n        </div>\n        \n        <div className=\"mt-4 flex justify-end\">\n          <button\n            onClick={resetFilters}\n            className=\"px-4 py-2 bg-gray-100 text-gray-700 rounded-md hover:bg-gray-200 mr-2\"\n          >\n            초기화\n          </button>\n        </div>\n      </div>\n      \n      {/* 테이블 */}\n      <div className=\"overflow-x-auto\">\n        <table className=\"min-w-full bg-white border border-gray-200\">\n          <thead>\n            <tr className=\"bg-gray-100\">\n              <th \n                className=\"py-3 px-4 border-b text-left cursor-pointer\"\n                onClick={() => handleSort('created_at')}\n              >\n                작업일 {sortField === 'created_at' && (\n                  <span>{sortDirection === 'asc' ? '↑' : '↓'}</span>\n                )}\n              </th>\n              <th \n                className=\"py-3 px-4 border-b text-left cursor-pointer\"\n                onClick={() => handleSort('customer_name')}\n              >\n                고객/장비 {sortField === 'customer_name' && (\n                  <span>{sortDirection === 'asc' ? '↑' : '↓'}</span>\n                )}\n              </th>\n              <th className=\"py-3 px-4 border-b text-left\">🔧 ECU 정보</th>\n              <th className=\"py-3 px-4 border-b text-left\">⚙️ ACU 정보</th>\n              <th \n                className=\"py-3 px-4 border-b text-left cursor-pointer\"\n                onClick={() => handleSort('status')}\n              >\n                상태 {sortField === 'status' && (\n                  <span>{sortDirection === 'asc' ? '↑' : '↓'}</span>\n                )}\n              </th>\n              <th \n                className=\"py-3 px-4 border-b text-left cursor-pointer\"\n                onClick={() => handleSort('amount')}\n              >\n                금액 {sortField === 'amount' && (\n                  <span>{sortDirection === 'asc' ? '↑' : '↓'}</span>\n                )}\n              </th>\n              <th className=\"py-3 px-4 border-b text-left\">작업</th>\n            </tr>\n          </thead>\n          <tbody>\n            {/* 테이블 행 컴포넌트 사용 */}\n            {loading ? (\n              <tr>\n                <td colSpan={7} className=\"py-4 text-center\">로딩 중...</td>\n              </tr>\n            ) : filteredRecords.length === 0 ? (\n              <tr>\n                <td colSpan={7} className=\"py-4 text-center\">검색 결과가 없습니다.</td>\n              </tr>\n            ) : (\n              filteredRecords.map((record) => (\n                <WorkRecordRow \n                  key={record.id} \n                  record={record} \n                  onRowClick={handleRowClick} \n                />\n              ))\n            )}\n          </tbody>\n        </table>\n      </div>\n      \n      {/* 페이지네이션 및 모달 컴포넌트는 생략 */}\n    </div>\n  );\n};\n\nexport default HistoryPage;\n```\n\n이 구현은 클라이언트 측에서 정렬 및 필터링을 수행하므로 서버 요청 없이 즉각적인 결과를 제공합니다. 데이터 양이 많아지면 서버 측 필터링으로 전환할 수 있습니다.",
        "testStrategy": "1. 각 컬럼 헤더 클릭 시 정렬이 정상 작동하는지 확인\n2. 정렬 방향 전환(오름차순/내림차순)이 정확히 작동하는지 테스트\n3. ECU/ACU 타입 필터가 정확히 데이터를 필터링하는지 검증\n4. 고객명 검색이 부분 일치로 정상 작동하는지 확인\n5. 날짜 범위 필터가 정확히 작동하는지 테스트\n6. 필터 초기화 버튼이 모든 필터를 리셋하는지 확인\n7. 필터링된 결과가 없을 때 적절한 메시지가 표시되는지 검증",
        "priority": "medium",
        "dependencies": [
          2,
          3,
          4
        ],
        "status": "pending",
        "subtasks": []
      },
      {
        "id": 9,
        "title": "테이블 페이지네이션 구현",
        "description": "대량의 작업 이력 데이터를 효율적으로 표시하기 위한 페이지네이션 기능 구현",
        "details": "테이블 하단에 페이지네이션 컨트롤을 추가하여 대량의 작업 이력 데이터를 페이지 단위로 나누어 표시합니다. 이를 통해 페이지 로딩 시간을 단축하고 사용자 경험을 향상시킵니다.\n\n```tsx\n// HistoryPage 컴포넌트 내부에 추가\nconst [pagination, setPagination] = useState({\n  currentPage: 1,\n  itemsPerPage: 10, // 페이지당 표시할 항목 수\n  totalPages: 1\n});\n\n// 페이지네이션 적용 로직 (useEffect 내부에 추가)\nuseEffect(() => {\n  // 필터링 및 정렬 로직 (기존 코드)\n  // ...\n  \n  // 페이지네이션 계산\n  const totalItems = result.length;\n  const totalPages = Math.ceil(totalItems / pagination.itemsPerPage);\n  \n  // 현재 페이지가 총 페이지 수를 초과하면 조정\n  const currentPage = Math.min(pagination.currentPage, totalPages || 1);\n  \n  // 현재 페이지에 해당하는 항목만 선택\n  const startIndex = (currentPage - 1) * pagination.itemsPerPage;\n  const paginatedItems = result.slice(startIndex, startIndex + pagination.itemsPerPage);\n  \n  setFilteredRecords(paginatedItems);\n  setPagination(prev => ({\n    ...prev,\n    currentPage,\n    totalPages\n  }));\n}, [records, filters, sortField, sortDirection, pagination.itemsPerPage, pagination.currentPage]);\n\n// 페이지 변경 핸들러\nconst handlePageChange = (newPage) => {\n  setPagination(prev => ({\n    ...prev,\n    currentPage: newPage\n  }));\n};\n\n// 페이지당 항목 수 변경 핸들러\nconst handleItemsPerPageChange = (e) => {\n  const newItemsPerPage = parseInt(e.target.value);\n  setPagination(prev => ({\n    ...prev,\n    itemsPerPage: newItemsPerPage,\n    currentPage: 1 // 항목 수 변경 시 첫 페이지로 이동\n  }));\n};\n\n// 페이지네이션 컴포넌트\nconst Pagination = () => {\n  const { currentPage, totalPages } = pagination;\n  \n  // 표시할 페이지 버튼 범위 계산\n  const pageRange = 5; // 한 번에 표시할 페이지 버튼 수\n  const startPage = Math.max(1, currentPage - Math.floor(pageRange / 2));\n  const endPage = Math.min(totalPages, startPage + pageRange - 1);\n  \n  const pageButtons = [];\n  for (let i = startPage; i <= endPage; i++) {\n    pageButtons.push(\n      <button\n        key={i}\n        onClick={() => handlePageChange(i)}\n        className={`px-3 py-1 mx-1 rounded ${currentPage === i ? 'bg-blue-500 text-white' : 'bg-gray-100'}`}\n      >\n        {i}\n      </button>\n    );\n  }\n  \n  return (\n    <div className=\"flex items-center justify-between mt-4\">\n      <div className=\"flex items-center\">\n        <span className=\"mr-2\">페이지당 항목:</span>\n        <select\n          value={pagination.itemsPerPage}\n          onChange={handleItemsPerPageChange}\n          className=\"border rounded px-2 py-1\"\n        >\n          <option value=\"10\">10</option>\n          <option value=\"20\">20</option>\n          <option value=\"50\">50</option>\n          <option value=\"100\">100</option>\n        </select>\n      </div>\n      \n      <div className=\"flex items-center\">\n        <button\n          onClick={() => handlePageChange(1)}\n          disabled={currentPage === 1}\n          className=\"px-3 py-1 mx-1 rounded bg-gray-100 disabled:opacity-50\"\n        >\n          &lt;&lt;\n        </button>\n        <button\n          onClick={() => handlePageChange(currentPage - 1)}\n          disabled={currentPage === 1}\n          className=\"px-3 py-1 mx-1 rounded bg-gray-100 disabled:opacity-50\"\n        >\n          &lt;\n        </button>\n        \n        {pageButtons}\n        \n        <button\n          onClick={() => handlePageChange(currentPage + 1)}\n          disabled={currentPage === totalPages}\n          className=\"px-3 py-1 mx-1 rounded bg-gray-100 disabled:opacity-50\"\n        >\n          &gt;\n        </button>\n        <button\n          onClick={() => handlePageChange(totalPages)}\n          disabled={currentPage === totalPages}\n          className=\"px-3 py-1 mx-1 rounded bg-gray-100 disabled:opacity-50\"\n        >\n          &gt;&gt;\n        </button>\n      </div>\n      \n      <div>\n        <span className=\"text-sm text-gray-500\">\n          {totalPages > 0 ? `${currentPage} / ${totalPages} 페이지` : '0 페이지'}\n        </span>\n      </div>\n    </div>\n  );\n};\n\n// 테이블 하단에 페이지네이션 추가\n<div className=\"mt-4\">\n  <Pagination />\n</div>\n```\n\n이 구현은 클라이언트 측 페이지네이션을 사용하며, 필터링 및 정렬 후 결과에 대해 페이지네이션을 적용합니다. 데이터 양이 많아지면 서버 측 페이지네이션으로 전환할 수 있습니다.",
        "testStrategy": "1. 페이지 버튼 클릭 시 해당 페이지의 데이터가 정확히 표시되는지 확인\n2. 이전/다음 페이지 버튼이 정상 작동하는지 테스트\n3. 첫 페이지/마지막 페이지 버튼이 정상 작동하는지 검증\n4. 페이지당 항목 수 변경 시 데이터가 정확히 업데이트되는지 확인\n5. 필터링 후 페이지네이션이 정확히 조정되는지 테스트\n6. 첫 페이지나 마지막 페이지에서 비활성화되어야 할 버튼이 제대로 비활성화되는지 확인",
        "priority": "medium",
        "dependencies": [
          8
        ],
        "status": "pending",
        "subtasks": []
      },
      {
        "id": 10,
        "title": "스켈레톤 UI 및 로딩 상태 개선",
        "description": "데이터 로딩 중 사용자 경험을 향상시키기 위한 스켈레톤 UI 구현",
        "details": "데이터 로딩 중에 단순한 로딩 메시지 대신 테이블 구조를 유지하는 스켈레톤 UI를 표시하여 사용자 경험을 향상시킵니다. 이를 통해 사용자는 데이터가 로드되는 동안 페이지 레이아웃을 미리 파악할 수 있습니다.\n\n```tsx\n// 스켈레톤 테이블 행 컴포넌트\nconst SkeletonRow = () => (\n  <tr className=\"animate-pulse\">\n    <td className=\"py-3 px-4 border-b\">\n      <div className=\"h-4 bg-gray-200 rounded w-24\"></div>\n    </td>\n    <td className=\"py-3 px-4 border-b\">\n      <div className=\"h-4 bg-gray-200 rounded w-32 mb-2\"></div>\n      <div className=\"h-3 bg-gray-200 rounded w-24\"></div>\n    </td>\n    <td className=\"py-3 px-4 border-b\">\n      <div className=\"h-4 bg-gray-200 rounded w-28 mb-2\"></div>\n      <div className=\"h-3 bg-gray-200 rounded w-20\"></div>\n    </td>\n    <td className=\"py-3 px-4 border-b\">\n      <div className=\"h-4 bg-gray-200 rounded w-28 mb-2\"></div>\n      <div className=\"h-3 bg-gray-200 rounded w-20\"></div>\n    </td>\n    <td className=\"py-3 px-4 border-b\">\n      <div className=\"h-6 bg-gray-200 rounded w-16\"></div>\n    </td>\n    <td className=\"py-3 px-4 border-b\">\n      <div className=\"h-4 bg-gray-200 rounded w-20\"></div>\n    </td>\n    <td className=\"py-3 px-4 border-b\">\n      <div className=\"h-8 bg-gray-200 rounded w-20\"></div>\n    </td>\n  </tr>\n);\n\n// 테이블 tbody 내부 로딩 상태 처리 수정\n{loading ? (\n  // 스켈레톤 UI 표시\n  Array(5).fill(0).map((_, index) => (\n    <SkeletonRow key={`skeleton-${index}`} />\n  ))\n) : filteredRecords.length === 0 ? (\n  <tr>\n    <td colSpan={7} className=\"py-4 text-center\">검색 결과가 없습니다.</td>\n  </tr>\n) : (\n  filteredRecords.map((record) => (\n    <WorkRecordRow \n      key={record.id} \n      record={record} \n      onRowClick={handleRowClick} \n    />\n  ))\n)}\n```\n\n파일 다운로드 섹션의 로딩 상태도 개선합니다:\n\n```tsx\n// FileDownloadSection 컴포넌트 내부 로딩 상태 처리 수정\n{loading ? (\n  <div className=\"animate-pulse space-y-2\">\n    {Array(3).fill(0).map((_, index) => (\n      <div key={`file-skeleton-${index}`} className=\"flex items-center justify-between bg-white p-2 rounded border\">\n        <div className=\"flex items-center\">\n          <div className=\"w-5 h-5 bg-gray-200 rounded mr-2\"></div>\n          <div className=\"h-4 bg-gray-200 rounded w-48\"></div>\n        </div>\n        <div className=\"h-6 bg-gray-200 rounded w-20\"></div>\n      </div>\n    ))}\n  </div>\n) : error ? (\n  <p className=\"text-red-500\">{error}</p>\n) : files.length === 0 ? (\n  <p className=\"text-gray-400 italic\">업로드된 파일이 없습니다.</p>\n) : (\n  // 기존 파일 목록 표시 코드\n)}\n```\n\n또한 전역 로딩 상태 표시기를 추가하여 API 호출 중임을 사용자에게 알립니다:\n\n```tsx\n// 전역 로딩 인디케이터 컴포넌트\nconst LoadingIndicator = ({ isLoading }) => {\n  if (!isLoading) return null;\n  \n  return (\n    <div className=\"fixed top-4 right-4 bg-blue-500 text-white px-4 py-2 rounded-md shadow-md z-50 flex items-center\">\n      <svg className=\"animate-spin -ml-1 mr-2 h-4 w-4 text-white\" xmlns=\"http://www.w3.org/2000/svg\" fill=\"none\" viewBox=\"0 0 24 24\">\n        <circle className=\"opacity-25\" cx=\"12\" cy=\"12\" r=\"10\" stroke=\"currentColor\" strokeWidth=\"4\"></circle>\n        <path className=\"opacity-75\" fill=\"currentColor\" d=\"M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z\"></path>\n      </svg>\n      데이터 로딩 중...\n    </div>\n  );\n};\n\n// 메인 컴포넌트에 통합\n<>\n  <LoadingIndicator isLoading={loading} />\n  {/* 기존 컴포넌트 */}\n</>\n```\n\n이러한 개선은 사용자에게 데이터 로딩 상태에 대한 명확한 시각적 피드백을 제공하여 사용자 경험을 향상시킵니다.",
        "testStrategy": "1. 데이터 로딩 중 스켈레톤 UI가 정상적으로 표시되는지 확인\n2. 스켈레톤 UI가 실제 데이터 구조와 유사하게 표시되는지 검증\n3. 로딩 완료 후 스켈레톤 UI가 실제 데이터로 부드럽게 전환되는지 테스트\n4. 전역 로딩 인디케이터가 API 호출 중에 정확히 표시되는지 확인\n5. 다양한 네트워크 상태(느린 연결 등)에서 로딩 UI가 적절히 작동하는지 테스트\n6. 모바일 환경에서도 스켈레톤 UI가 적절히 표시되는지 확인",
        "priority": "low",
        "dependencies": [
          2,
          6
        ],
        "status": "pending",
        "subtasks": []
      },
      {
        "id": 11,
        "title": "에러 처리 및 사용자 피드백 개선",
        "description": "데이터 로딩 및 파일 다운로드 중 발생할 수 있는 오류에 대한 처리와 사용자 피드백 개선",
        "details": "데이터 로딩 및 파일 다운로드 중 발생할 수 있는 다양한 오류 상황에 대한 처리를 개선하고, 사용자에게 명확한 피드백을 제공합니다. 토스트 메시지를 활용하여 성공 및 오류 상태를 알립니다.\n\n먼저 react-hot-toast 라이브러리를 설치합니다:\n\n```bash\nnpm install react-hot-toast\n# 또는\nyarn add react-hot-toast\n```\n\n그리고 다음과 같이 구현합니다:\n\n```tsx\nimport { Toaster, toast } from 'react-hot-toast';\n\n// 메인 컴포넌트 내부에 Toaster 추가\n<>\n  <Toaster position=\"top-right\" />\n  {/* 기존 컴포넌트 */}\n</>\n\n// 데이터 로딩 에러 처리 개선\nconst loadData = async () => {\n  setLoading(true);\n  try {\n    const data = await fetchWorkRecords();\n    setRecords(data);\n    setFilteredRecords(data);\n  } catch (error) {\n    console.error('데이터 로딩 오류:', error);\n    toast.error('작업 이력을 불러오는 중 오류가 발생했습니다. 잠시 후 다시 시도해주세요.');\n    // 빈 배열로 설정하여 UI가 깨지지 않도록 함\n    setRecords([]);\n    setFilteredRecords([]);\n  } finally {\n    setLoading(false);\n  }\n};\n\n// 파일 다운로드 에러 처리 개선 (FileDownloadSection 컴포넌트 내부)\nconst handleDownload = async (file) => {\n  try {\n    // 다운로드 시작 알림\n    const toastId = toast.loading(`${file.original_name} 다운로드 중...`);\n    \n    // Supabase Storage에서 파일 URL 가져오기\n    const { data, error } = await supabase\n      .storage\n      .from('work_files')\n      .createSignedUrl(file.storage_path, 60);\n      \n    if (error) throw error;\n    \n    // 파일 다운로드 링크 열기\n    window.open(data.signedUrl, '_blank');\n    \n    // 성공 알림\n    toast.success(`${file.original_name} 다운로드 시작됨`, {\n      id: toastId\n    });\n  } catch (err) {\n    console.error('파일 다운로드 오류:', err);\n    \n    // 오류 유형에 따른 맞춤형 메시지\n    let errorMessage = '파일 다운로드 중 오류가 발생했습니다.';\n    \n    if (err.message?.includes('not found')) {\n      errorMessage = '파일을 찾을 수 없습니다. 관리자에게 문의하세요.';\n    } else if (err.message?.includes('permission')) {\n      errorMessage = '파일 다운로드 권한이 없습니다.';\n    } else if (err.message?.includes('network')) {\n      errorMessage = '네트워크 연결을 확인해주세요.';\n    }\n    \n    toast.error(errorMessage);\n  }\n};\n\n// 일괄 다운로드 에러 처리 개선\nconst handleBulkDownload = async () => {\n  if (files.length === 0) return;\n  \n  const toastId = toast.loading('파일 준비 중...');\n  \n  try {\n    setLoading(true);\n    \n    const zip = new JSZip();\n    const promises = [];\n    let successCount = 0;\n    let failCount = 0;\n    \n    // 각 파일에 대한 다운로드 Promise 생성\n    for (const file of files) {\n      const promise = new Promise(async (resolve, reject) => {\n        try {\n          // Supabase Storage에서 파일 URL 가져오기\n          const { data, error } = await supabase\n            .storage\n            .from('work_files')\n            .createSignedUrl(file.storage_path, 60);\n            \n          if (error) throw error;\n          \n          // 파일 다운로드 및 ZIP에 추가\n          const response = await fetch(data.signedUrl);\n          if (!response.ok) throw new Error(`HTTP error! status: ${response.status}`);\n          \n          const blob = await response.blob();\n          zip.file(file.original_name, blob);\n          successCount++;\n          resolve();\n        } catch (err) {\n          console.error(`파일 '${file.original_name}' 다운로드 오류:`, err);\n          failCount++;\n          // 개별 파일 오류는 전체 프로세스를 중단하지 않음\n          resolve();\n        }\n      });\n      \n      promises.push(promise);\n    }\n    \n    // 진행 상태 업데이트\n    toast.loading(`파일 다운로드 중... (0/${files.length})`, { id: toastId });\n    \n    // 모든 파일 다운로드 완료 대기\n    await Promise.all(promises);\n    \n    if (successCount === 0) {\n      throw new Error('다운로드할 수 있는 파일이 없습니다.');\n    }\n    \n    // ZIP 파일 생성 및 다운로드\n    toast.loading(`ZIP 파일 생성 중...`, { id: toastId });\n    const content = await zip.generateAsync({ type: 'blob' });\n    const timestamp = new Date().toISOString().replace(/[:.]/g, '-');\n    saveAs(content, `work-files-${recordId}-${timestamp}.zip`);\n    \n    // 결과 알림\n    if (failCount > 0) {\n      toast.success(`ZIP 파일 생성 완료 (${successCount}개 성공, ${failCount}개 실패)`, { id: toastId });\n    } else {\n      toast.success('모든 파일이 ZIP으로 다운로드되었습니다.', { id: toastId });\n    }\n  } catch (err) {\n    console.error('일괄 다운로드 오류:', err);\n    toast.error(err.message || '파일 일괄 다운로드 중 오류가 발생했습니다.', { id: toastId });\n  } finally {\n    setLoading(false);\n  }\n};\n```\n\n또한 오프라인 상태 감지 및 처리를 추가합니다:\n\n```tsx\n// 메인 컴포넌트 내부에 오프라인 상태 감지 추가\nconst [isOffline, setIsOffline] = useState(false);\n\nuseEffect(() => {\n  const handleOnline = () => {\n    setIsOffline(false);\n    toast.success('온라인 상태로 전환되었습니다.');\n    // 데이터 다시 로드\n    loadData();\n  };\n  \n  const handleOffline = () => {\n    setIsOffline(true);\n    toast.error('오프라인 상태입니다. 네트워크 연결을 확인해주세요.');\n  };\n  \n  window.addEventListener('online', handleOnline);\n  window.addEventListener('offline', handleOffline);\n  \n  // 초기 상태 확인\n  setIsOffline(!navigator.onLine);\n  \n  return () => {\n    window.removeEventListener('online', handleOnline);\n    window.removeEventListener('offline', handleOffline);\n  };\n}, []);\n\n// 오프라인 상태 배너 표시\n{isOffline && (\n  <div className=\"bg-red-100 border-l-4 border-red-500 text-red-700 p-4 mb-4\">\n    <div className=\"flex items-center\">\n      <svg className=\"h-5 w-5 mr-2\" fill=\"currentColor\" viewBox=\"0 0 20 20\">\n        <path fillRule=\"evenodd\" d=\"M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7 4a1 1 0 11-2 0 1 1 0 012 0zm-1-9a1 1 0 00-1 1v4a1 1 0 102 0V6a1 1 0 00-1-1z\" clipRule=\"evenodd\" />\n      </svg>\n      <p>오프라인 상태입니다. 일부 기능이 제한될 수 있습니다.</p>\n    </div>\n  </div>\n)}\n```\n\n이러한 개선은 사용자에게 다양한 상황에 대한 명확한 피드백을 제공하여 사용자 경험을 향상시킵니다.",
        "testStrategy": "1. 데이터 로딩 실패 시 적절한 오류 메시지가 표시되는지 확인\n2. 파일 다운로드 성공/실패 시 토스트 메시지가 정확히 표시되는지 테스트\n3. 다양한 오류 상황(권한 없음, 파일 없음, 네트워크 오류 등)에 대해 맞춤형 메시지가 표시되는지 검증\n4. 오프라인 상태 감지 및 알림이 정상 작동하는지 테스트\n5. 온라인 상태로 전환 시 데이터가 자동으로 다시 로드되는지 확인\n6. 일괄 다운로드 중 진행 상태가 정확히 표시되는지 테스트",
        "priority": "medium",
        "dependencies": [
          6,
          7
        ],
        "status": "pending",
        "subtasks": []
      },
      {
        "id": 12,
        "title": "최종 UI 폴리싱 및 반응형 디자인 최적화",
        "description": "전체 UI의 일관성을 확보하고 다양한 화면 크기에서 최적의 사용자 경험을 제공하기 위한 최종 폴리싱",
        "details": "전체 UI의 일관성을 확보하고, 다양한 화면 크기(데스크톱, 태블릿, 모바일)에서 최적의 사용자 경험을 제공하기 위한 최종 폴리싱 작업을 수행합니다. 색상, 폰트, 간격 등을 일관되게 조정하고, 반응형 디자인을 최적화합니다.\n\n```tsx\n// 테이블 반응형 디자인 최적화\n<div className=\"overflow-x-auto shadow rounded-lg\">\n  <table className=\"min-w-full bg-white\">\n    {/* 테이블 내용 */}\n  </table>\n</div>\n\n// 모바일 화면에서 테이블 컬럼 우선순위 설정\n// WorkRecordRow 컴포넌트 내부\n<tr key={record.id} className=\"hover:bg-gray-50 cursor-pointer border-b\" onClick={() => onRowClick(record)}>\n  <td className=\"py-3 px-4\">{formattedDate}</td>\n  <td className=\"py-3 px-4\">\n    <div className=\"font-medium\">{record.customer_name}</div>\n    <div className=\"text-sm text-gray-500\">{record.vehicle_model}</div>\n  </td>\n  <td className=\"py-3 px-4 hidden md:table-cell\"> {/* 모바일에서는 숨김 */}\n    <ECUInfoCell ecuData={record.ecu_data} />\n  </td>\n  <td className=\"py-3 px-4 hidden md:table-cell\"> {/* 모바일에서는 숨김 */}\n    <ACUInfoCell acuData={record.acu_data} />\n  </td>\n  <td className=\"py-3 px-4\">\n    <span className={`px-2 py-1 rounded-full text-xs ${getStatusColor(record.status)}`}>\n      {record.status}\n    </span>\n  </td>\n  <td className=\"py-3 px-4 hidden sm:table-cell\"> {/* 작은 모바일에서는 숨김 */}\n    {new Intl.NumberFormat('ko-KR', { style: 'currency', currency: 'KRW' }).format(record.amount)}\n  </td>\n  <td className=\"py-3 px-4\">\n    <button className=\"text-blue-500 hover:text-blue-700\">\n      상세보기\n    </button>\n  </td>\n</tr>\n```\n\n테마 색상 변수를 정의하여 일관된 색상 적용을 보장합니다:\n\n```css\n/* globals.css 또는 tailwind.config.js에 추가 */\n:root {\n  --color-primary: #3b82f6; /* 파란색 계열 */\n  --color-primary-light: #93c5fd;\n  --color-secondary: #10b981; /* 초록색 계열 */\n  --color-secondary-light: #6ee7b7;\n  --color-background: #f9fafb;\n  --color-surface: #ffffff;\n  --color-text: #1f2937;\n  --color-text-light: #6b7280;\n}\n\n/* Tailwind 설정에 추가 */\ntheme: {\n  extend: {\n    colors: {\n      primary: {\n        DEFAULT: 'var(--color-primary)',\n        light: 'var(--color-primary-light)',\n      },\n      secondary: {\n        DEFAULT: 'var(--color-secondary)',\n        light: 'var(--color-secondary-light)',\n      },\n    },\n  },\n},\n```\n\n모바일 화면에서의 필터 UI 최적화:\n\n```tsx\n// 필터 컨트롤 반응형 디자인 최적화\n<div className=\"mb-6 bg-white p-4 rounded shadow\">\n  <div className=\"grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4\">\n    {/* 필터 컨트롤 내용 */}\n  </div>\n</div>\n```\n\n접근성 개선을 위한 ARIA 속성 추가:\n\n```tsx\n// 테이블에 접근성 속성 추가\n<table className=\"min-w-full bg-white\" role=\"grid\" aria-label=\"작업 이력 테이블\">\n  <thead>\n    <tr className=\"bg-gray-100\" role=\"row\">\n      <th className=\"py-3 px-4 border-b text-left cursor-pointer\" role=\"columnheader\" aria-sort={sortField === 'created_at' ? sortDirection : 'none'} onClick={() => handleSort('created_at')}>\n        작업일 {sortField === 'created_at' && (\n          <span aria-hidden=\"true\">{sortDirection === 'asc' ? '↑' : '↓'}</span>\n        )}\n      </th>\n      {/* 다른 헤더 셀들 */}\n    </tr>\n  </thead>\n  <tbody role=\"rowgroup\">\n    {/* 테이블 행들 */}\n  </tbody>\n</table>\n```\n\n다크 모드 지원 (선택적):\n\n```tsx\n// 다크 모드 토글 버튼\nconst [darkMode, setDarkMode] = useState(false);\n\nuseEffect(() => {\n  // 사용자 시스템 설정 확인\n  const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;\n  setDarkMode(prefersDark);\n  \n  // 다크 모드 클래스 적용\n  if (prefersDark) {\n    document.documentElement.classList.add('dark');\n  }\n}, []);\n\nconst toggleDarkMode = () => {\n  setDarkMode(!darkMode);\n  document.documentElement.classList.toggle('dark');\n};\n\n// 다크 모드 토글 버튼 추가\n<button\n  onClick={toggleDarkMode}\n  className=\"fixed bottom-4 right-4 p-2 rounded-full bg-gray-200 dark:bg-gray-700\"\n  aria-label=\"다크 모드 전환\"\n>\n  {darkMode ? (\n    <svg className=\"w-6 h-6\" fill=\"currentColor\" viewBox=\"0 0 20 20\">\n      <path d=\"M10 2a1 1 0 011 1v1a1 1 0 11-2 0V3a1 1 0 011-1zm4 8a4 4 0 11-8 0 4 4 0 018 0zm-.464 4.95l.707.707a1 1 0 001.414-1.414l-.707-.707a1 1 0 00-1.414 1.414zm2.12-10.607a1 1 0 010 1.414l-.706.707a1 1 0 11-1.414-1.414l.707-.707a1 1 0 011.414 0zM17 11a1 1 0 100-2h-1a1 1 0 100 2h1zm-7 4a1 1 0 011 1v1a1 1 0 11-2 0v-1a1 1 0 011-1zM5.05 6.464A1 1 0 106.465 5.05l-.708-.707a1 1 0 00-1.414 1.414l.707.707zm1.414 8.486l-.707.707a1 1 0 01-1.414-1.414l.707-.707a1 1 0 011.414 1.414zM4 11a1 1 0 100-2H3a1 1 0 000 2h1z\" fillRule=\"evenodd\" clipRule=\"evenodd\" />\n    </svg>\n  ) : (\n    <svg className=\"w-6 h-6\" fill=\"currentColor\" viewBox=\"0 0 20 20\">\n      <path d=\"M17.293 13.293A8 8 0 016.707 2.707a8.001 8.001 0 1010.586 10.586z\" />\n    </svg>\n  )}\n</button>\n```\n\n이러한 최종 폴리싱 작업은 전체 UI의 일관성을 확보하고, 다양한 화면 크기에서 최적의 사용자 경험을 제공합니다.",
        "testStrategy": "1. 다양한 화면 크기(데스크톱, 태블릿, 모바일)에서 UI가 적절히 표시되는지 확인\n2. 모바일 화면에서 중요 정보가 우선적으로 표시되는지 검증\n3. 색상, 폰트, 간격 등이 일관되게 적용되었는지 테스트\n4. 접근성 도구(스크린 리더 등)로 테이블이 올바르게 읽히는지 확인\n5. 다크 모드 전환이 정상 작동하는지 테스트 (구현한 경우)\n6. 사용자 제공 스크린샷과 최종 UI를 비교하여 일치도 확인",
        "priority": "low",
        "dependencies": [
          3,
          4,
          5,
          6,
          8,
          9,
          10,
          11
        ],
        "status": "pending",
        "subtasks": []
      }
    ],
    "metadata": {
      "created": "2025-08-04T18:34:25.065Z",
      "updated": "2025-08-04T18:34:25.065Z",
      "description": "Tasks for work-history-improvement context"
    }
  }
}